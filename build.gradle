// Root build.gradle for OpenCameraPro (modernized for AGP 8.x)

plugins {
    id 'com.android.application' version '8.3.2' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
